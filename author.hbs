{{!< default}}

<div class="gh-page">
    <div class="gh-container">

        {{#author}}
        <header class="gh-page-head">
            <div class="author-head-wrapper" style="background-image: url('{{img_url cover_image}}');>
                <div class="author-bio-offset">
                    {{#if profile_image}}
                        <img class="gh-author-image" src="{{profile_image}}" alt="{{name}}" />
                    {{/if}}

                    <h1>{{name}}</h1>
                    {{#if bio}}
                        <p>{{bio}}</p>
                    {{/if}}
                </div>
            </div>

            <div class="gh-author-meta">
                <div class="gh-author-links">
                    {{#if website}}
                        <a href="{{website}}" target="_blank" rel="noopener">Website</a>
                    {{/if}}
                    {{#if twitter}}
                        <a href="{{twitter_url}}" target="_blank" rel="noopener">Twitter</a>
                    {{/if}}
                    {{#if facebook}}
                        <a href="{{facebook_url}}" target="_blank" rel="noopener">Facebook</a>
                    {{/if}}
                </div>
            </div>
        </header>
        {{/author}}

    </div>
</div>